import React from "react";
import ReactDOM from "react-dom/client";
import ThinkType from "./ThinkType";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ThinkType />
  </React.StrictMode>
);
