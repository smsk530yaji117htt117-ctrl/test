services:
  - type: cron
    name: personal-os-consensus
    env: python
    runtime: python3
    schedule: "*/10 * * * *"
    buildCommand: pip install -r requirements.txt
    startCommand: python consensus.py
