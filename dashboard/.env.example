# PersonalOS 読み取り専用ダッシュボード — 環境変数サンプル
#
# 実際の値を入れたファイルは `.env` として保存してください（コミット禁止 / .gitignore 済み）。
# このトークンの Notion インテグレーションを、対象5DBに「接続（Connections）」しておくこと。
#
# 例: cp .env.example .env && エディタで NOTION_TOKEN を設定

NOTION_TOKEN=ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
